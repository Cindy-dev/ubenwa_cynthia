import 'package:flutter/material.dart';

class MilkSplashScreen extends StatelessWidget {
  const MilkSplashScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}